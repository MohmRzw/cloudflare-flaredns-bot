# 🔥 Flare DNS

## ربات تلگرام برای مدیریت حرفه‌ای DNS کلودفلر

مدیریت DNS و دامنه‌های Cloudflare، بدون نیاز به ورود به پنل — همه داخل تلگرام!
Flare DNS یک ربات قدرتمند، امن و چنداکانته است که با رابط ساده تلگرام، مدیریت دامنه‌ها و رکوردهای DNS را فوق‌العاده ساده میکند.

---


## ویدیو آموزشی
به زودی...
> برای مشاهده ویدیو روی تصویر بالا کلیک کنید.
---
## 🚀 نصب سریع (Quick Install)

اگر می‌خواهید بدون دانلود مخزن و با یک دستور ربات را نصب کنید:

```bash
sudo bash -c "$(curl -sSL https://raw.githubusercontent.com/MohmRzw/cloudflare-flaredns-bot/main/install.sh)"
```

---

## ⚡ روش جایگزین (Alternative Install)

اگر روش بالا روی سیستم شما اجرا نشد، از نصب معمولی استفاده کنید:

```bash
git clone https://github.com/MohmRzw/cloudflare-flaredns-bot.git \
&& cd cloudflare-flaredns-bot \
&& chmod +x install.sh \
&& sudo ./install.sh
```
---
### اسکریپت `install.sh` به‌صورت خودکار:

* ✅ نصب پیش‌نیازها: Python، venv، pip، screen
* ✅ ساخت محیط مجازی پایتون (venv)
* ✅ ساخت تعاملی فایل تنظیمات `config.py` شامل:

  * BOT_TOKEN
  * ADMIN_ID
  * API_URL (با مقدار پیش‌فرض)
  * ACCOUNTS_FILE (با مقدار پیش‌فرض)
* ✅ نمایش یک منوی گرافیکی ترمینال برای:

  * اجرای ربات به‌صورت عادی
  * اجرای ربات داخل screen (پیشنهادی – برای همیشه روشن)

---

# ✨ ویژگی‌ها (Features)

## 🌐 مدیریت چندین اکانت Cloudflare

* ذخیره و مدیریت چندین Cloudflare API Token در `accounts.json`
* سوئیچ بین اکانت‌ها از طریق منوی 👤 *مدیریت اکانت‌ها* در ربات

## 🧾 مدیریت دامنه‌ها (Zones)

* نمایش لیست کامل دامنه‌های هر اکانت
* صفحه‌بندی لیست برای سهولت
* نمایش وضعیت دامنه:

  * 🟢 Active
  * 🟡 Pending

## 🧱 مدیریت رکوردهای DNS

* مشاهده رکوردهای DNS شامل: A, AAAA, CNAME, TXT, MX, NS
* افزودن رکورد با ویزارد مرحله‌ای داخل تلگرام
* ویرایش تک‌فیلدی (Name / Content / TTL / Proxy)
* حذف رکورد با تأیید دوبل برای جلوگیری از اشتباه

## 📊 آمار کلی (Global Stats)

* تعداد کل دامنه‌ها
* تعداد دامنه‌های فعال
* تعداد دامنه‌های Pending

---

# 🧩 تنظیمات و اینستالر

اینستالر امکانات زیر را فراهم می‌کند:

* نصب کامل پیش‌نیازها
* ساخت و تنظیم فایل `config.py`
* اجرای ربات:

  * Foreground
  * screen session
* مدیریت Cloudflare API Tokens داخل `accounts.json`

  * مشاهده
  * افزودن/ویرایش
  * حذف

---

# 🔐 دسترسی امن (Security)

فقط کاربری که مقدار `ADMIN_ID` در `config.py` با آیدی عددی او یکسان باشد
اجازه استفاده از ربات را دارد.

تمام کاربران دیگر در صورت ارسال `/start` پاسخی دریافت نمی‌کنند.

---

# 🛠 پیش‌نیازها (Requirements)

* Linux (پیشنهاد: Ubuntu 22/24)
* Python 3.8+ (پیشنهاد: 3.10+)
* git
* screen
  *(در صورت نبود، اینستالر خودش نصب میکند)*

---

# ⚙️ فایل تنظیمات `config.py`

پس از نصب، نمونه فایل به شکل زیر است:

```python
BOT_TOKEN = "123456789:ABCDEF..."
ADMIN_ID = 2059978172
API_URL = "https://api.cloudflare.com/client/v4"
ACCOUNTS_FILE = "accounts.json"
ICONS = { ... }
```

تمام این مقادیر را می‌توان بدون ویرایش دستی فایل،
از طریق اینستالر و گزینه‌های ویرایش تغییر داد.

---

# 🔐 مدیریت Cloudflare Tokens (`accounts.json`)

نمونه ساختار فایل:

```json
{
  "main": "CF_API_TOKEN_1",
  "client1": "CF_API_TOKEN_2"
}
```

مدیریت این فایل از طریق اینستالر انجام می‌شود:

* 1️⃣ مشاهده توکن‌ها
* 2️⃣ افزودن/ویرایش توکن
* 3️⃣ حذف توکن

ربات نیز از همین فایل برای سوئیچ بین اکانت‌ها استفاده می‌کند.

---

# ▶️ اجرای ربات (Running the Bot)

## 1. اجرای معمولی (Foreground)

از طریق اینستالر:

```
6) Start Bot (Foreground)
```

برای توقف:

```
Ctrl + C
```

## 2. اجرای دائمی با screen (پیشنهادی)

```
8) Start Bot in Screen (Recommended)
```

اتصال به screen:

```bash
screen -r flaredns-bot
```

خروج بدون توقف:

```
Ctrl + A سپس D
```

توقف screen از اینستالر:

```
9) Stop Screen Session
```

---

# 📡 ارتباط و جامعه کاربری

👤 توسعه‌دهنده: **@mohmrzw**
📺 YouTube: [https://youtube.com/@mohmrzw](https://youtube.com/@mohmrzw)
📣 تلگرام (ExploreTechIR): [https://t.me/ExploreTechIR](https://t.me/ExploreTechIR)
💻 GitHub: [https://github.com/MohmRzw](https://github.com/MohmRzw)

اگر پروژه برات مفید بود، خوشحال میشم:

* ⭐️ ریپو رو Star کنی
* 🐛 هر باگ یا پیشنهادی داشتی تو Issues ثبت کنی
